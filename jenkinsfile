pipeline {
    agent any
    options {
        timeout(time: 1, unit: 'HOURS')
    }
    
    stages {
        stage ('checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/praveenkojha/first-jenkinsfile.git'
            }
        }
    }
}
